{{columns}}
{% for measurement in measurements %}
    {{ city }},{{ country }},{{ measurement.date | date('Y-m-d') }},{{ measurement.temperatureCelsius }},{{ measurement.temperatureFahrenheit }}
{% endfor %}